[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "graph-forge-mcp"
version = "0.1.0"
description = "Forge knowledge graphs MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
