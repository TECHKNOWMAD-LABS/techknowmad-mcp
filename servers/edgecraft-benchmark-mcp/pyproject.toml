[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "edgecraft-benchmark-mcp"
version = "0.1.0"
description = "Benchmark edge cases MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
