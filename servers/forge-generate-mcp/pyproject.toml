[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forge-generate-mcp"
version = "0.1.0"
description = "Generate code and artifacts MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
