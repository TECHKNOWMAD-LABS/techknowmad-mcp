[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "negativa-score"
version = "0.1.0"
description = "Score ideas by negatives and downsides MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
