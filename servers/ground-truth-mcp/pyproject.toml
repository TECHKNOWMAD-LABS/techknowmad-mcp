[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ground-truth-mcp"
version = "0.1.0"
description = "Validate claims against ground truth MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
