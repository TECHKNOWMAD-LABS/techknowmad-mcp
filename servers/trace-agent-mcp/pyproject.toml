[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "trace-agent-mcp"
version = "0.1.0"
description = "Trace agent execution paths MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
