[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "phyloid-evolve"
version = "0.1.0"
description = "Evolutionary and phylogenetic tree operations MCP server"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]
